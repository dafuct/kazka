// Package entry. Friendly aliases over generated OpenAPI types are added in
// Task 14 (after the codegen has produced api-types.ts). Runtime helpers
// (ApiError, error-code unions) live in ./errors.

export {};
